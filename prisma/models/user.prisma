enum UserRole {
  ADMIN
  USER
}

model User {
  id       String   @id @default(uuid())
  name     String
  password String
  email    String   @unique
  address  String?
  role     UserRole @default(USER)
}
